node_modules/
dist/
.next/
.turbo/
coverage/

*.tsbuildinfo
*.log

.env
.env.local
.env.production
.env.development

.DS_Store
*.pem

.vercel

# SQL is run manually against the DB now (no migrate.ts runner) — not tracked in git
packages/db/migrations/*.sql
