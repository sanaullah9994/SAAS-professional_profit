'use client';
import { Area,AreaChart,CartesianGrid,ResponsiveContainer,Tooltip,XAxis,YAxis } from 'recharts';
import type { ProfitTrendPoint } from '@amazon-profit/types';
import { Card,CardContent,Table,TableBody,TableCell,TableHead,TableHeader,TableRow } from '@amazon-profit/ui';
export function Chart({data}:{data:ProfitTrendPoint[]}){return <div className="h-80"><ResponsiveContainer><AreaChart data={data}><CartesianGrid vertical={false} strokeDasharray="3 3"/><XAxis dataKey="date" tickFormatter={v=>String(v).slice(5)}/><YAxis tickFormatter={v=>`$${Math.round(Number(v)/1000)}k`}/><Tooltip/><Area dataKey="revenue" stroke="var(--primary)" fill="var(--primary)" fillOpacity={.16}/><Area dataKey="profit" stroke="oklch(.62 .17 145)" fillOpacity={0}/></AreaChart></ResponsiveContainer></div>}
export function DataTable({headers,rows}:{headers:string[],rows:React.ReactNode[][]}){return <Table><TableHeader><TableRow>{headers.map(h=><TableHead key={h}>{h}</TableHead>)}</TableRow></TableHeader><TableBody>{rows.map((r,i)=><TableRow key={i}>{r.map((c,j)=><TableCell key={j}>{c}</TableCell>)}</TableRow>)}</TableBody></Table>}
export function PageHeader({title,description,action}:{title:string,description:string,action?:React.ReactNode}){return <div className="mb-6 flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><h1 className="text-2xl font-semibold">{title}</h1><p className="mt-1 text-sm text-muted-foreground">{description}</p></div>{action}</div>}
export function Metric({label,value,change}:{label:string,value:string,change:string}){return <Card><CardContent className="p-5"><p className="text-sm text-muted-foreground">{label}</p><div className="mt-4 flex items-end justify-between"><b className="text-2xl">{value}</b><span className="text-xs text-emerald-600">{change}</span></div></CardContent></Card>}
