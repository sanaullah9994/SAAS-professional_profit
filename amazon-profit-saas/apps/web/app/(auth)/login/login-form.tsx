'use client';
import { useState } from 'react';
import { useRouter } from 'next/navigation';
import { Button,Card,CardContent,CardDescription,CardHeader,CardTitle,Input } from '@amazon-profit/ui';
import { authClient } from '@/lib/auth-client';
export function LoginForm(){const router=useRouter(),[mode,setMode]=useState<'in'|'up'>('in'),[error,setError]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget),email=String(f.get('email')),password=String(f.get('password')),name=String(f.get('name')||'Amazon Seller');const r=mode==='in'?await authClient.signIn.email({email,password}):await authClient.signUp.email({email,password,name});if(r.error)return setError(r.error.message??'Authentication failed');router.push('/dashboard')}return <Card className="w-full max-w-md"><CardHeader><CardTitle>{mode==='in'?'Welcome back':'Create account'}</CardTitle><CardDescription>Email and password authentication through the NestJS API.</CardDescription></CardHeader><CardContent><form onSubmit={submit} className="space-y-4">{mode==='up'&&<Input name="name" placeholder="Name" required/>}<Input name="email" type="email" placeholder="Email" required/><Input name="password" type="password" placeholder="Password" minLength={8} required/>{error&&<p className="text-sm text-red-600">{error}</p>}<Button className="w-full">{mode==='in'?'Sign in':'Create account'}</Button></form><button className="mt-4 w-full text-sm text-primary" onClick={()=>setMode(mode==='in'?'up':'in')}>{mode==='in'?'Need an account? Sign up':'Already registered? Sign in'}</button></CardContent></Card>}
